# Design QA — agy-swap landing page

## Reference and tested state

- Selected reference: `/Users/akalakkruaboon/.codex/generated_images/01a030ae-0b1d-7010-8c5b-5807715b996b/exec-2845cb69-e085-4926-836b-5229fa319f46.png`
- Normalized reference evidence: `qa/reference-option-3.png`
- Implementation evidence: `qa/implementation-1536.jpg`
- Combined comparison: `qa/comparison-1536.png`
- Focused apparatus comparison: `qa/comparison-apparatus-focus.png`
- Mobile evidence: `qa/implementation-mobile.jpg`
- Desktop viewport: 1536 × 1024 CSS pixels at device scale factor 1.
- Mobile audit viewport: Lighthouse mobile emulation (412 × 823 CSS pixels, DPR 1.75); captured audit image is 250 × 498.
- Normalized comparison dimensions: 996 × 332; each side is 498 × 332.
- State: first load, Dashboard selected, static generated apparatus fallback, realistic sanitized sample data, live 3D not enabled.

## Comparison findings

The full-view comparison verifies the intended Option 3 structure: sparse navigation, left editorial uppercase headline, right install command and product copy, full-width titanium rail, carbon aperture, right precision dial, thin orange signal line, and the next feature section entering the fold. The production implementation intentionally places the real interactive TUI in the aperture and exposes its seven source-aligned views below it; no generated interface artwork is used.

The focused comparison confirms that the rail, aperture, dial, signal line, contrast, crop, and horizontal proportions retain the selected reference's visual hierarchy. The implementation aperture is slightly wider to keep the real TUI legible and interactive. This is an intentional product constraint, not a fidelity defect.

The mobile capture confirms a top-of-page first load, readable headline, working install CTA, 44px-class controls, and reflowed apparatus/TUI without horizontal clipping. A QA pass found and fixed initial TUI autofocus that had scrolled mobile users past the hero; focus now returns only after an interactive mode closes and uses `preventScroll`.

## Iteration history

1. Initial implementation centered the hero and reduced the apparatus to a generic framed demo. Classified P1 for composition and product-story mismatch.
2. Rebuilt the hero as the selected asymmetric editorial split, stretched the apparatus to the full rail composition, and inserted the real source-derived TUI. Classified P2 for apparatus scale and vertical rhythm.
3. Tightened hero padding/type scale, changed the apparatus to the 4:1 crop, aligned the terminal aperture and dial, and moved feature content into the fold.
4. Final responsive audit exposed automatic focus scrolling on mobile. Replaced initial autofocus with state-return focus using `preventScroll`, then repeated desktop/mobile visual and Lighthouse checks.
7. Full context-aware fixture matrix, performance splitting, and modal accessibility: Expanded Go fixture generator to 219 deterministic fixtures covering all 9 active × selected combinations for Dashboard and Quota, genuine post-delete models, Settings form over Settings, Profile form over Profiles, History confirm over History, and all 7-view Help/Palette overlays. Split initial paint fixtures (`tui-initial-fixtures.json`, 3 frames) from lazy full bank (`/tui-fixtures.json`), cutting main bundle JS down to 240.7 kB (70.0 kB gzip). Added measured aperture cell capacity layout selection, `data-layout` CSS aspect ratios (2:1, 5:3, 15:7), modal `role="dialog"` Tab-trapping, and mobile nav `<600px` link hiding with preserved install CTA.
8. Aperture aspect resolution, wide 100×24 geometry, and legible scale calibration: Adjusted wide fixture dimensions to 100×24 (innerCols 98), set `terminalOverlay` desktop width 56% with `height: auto`, tablet width 92% (≤960px), and mobile width 96% (≤600px), ensuring `data-layout` aspect-ratio strictly determines height without oversized vertical black bars. Calibrated scale to ~13px desktop and ≥10px mobile. Eliminated initial unscaled render flash via `isMeasured` opacity gating. Corrected `n` key to immediately switch active account in Dashboard and Quota. Removed stale `TUI.module.css` and duplicate public fixture file.
9. Deterministic countdown floor margin & complete runtime form/shortcut parity: Captured `renderNow` with 45-second floor margins to eliminate countdown time drift across runs. Expanded authoritative Go fixture generator to 396 fixtures covering all 11 real form kinds (`profile-create`, `profile-edit`, `tags`, `settings`, `alias`, `binding`, `target`, `history-export`, `backup-export`, `backup-import`, `backup-verify`) with every individual focused field index (0..len(Fields)-1). Added full `Up/Down/j/k/PageUp/PageDown/Home/End` navigation for accounts, profile indices, and history indices; 1-9 direct account selection; view shortcuts (`v` for Quota / backup-verify, `e/c/d/x/i/a/b/t/u`); Profiles removal confirmation with genuine post-remove state; and honest aria-live notifications. External view controls updated to accessible `role="toolbar"` with `aria-pressed`.
10. Scoped result states & context-preserving overlays (819 fixtures): Resolved state latching by scoping post-delete and post-profile-remove results strictly to initial action frames; subsequent navigation or view switching renders the requested destination with reduced account/profile collections and returns to standard ready states. Generated complete context-preserving overlays across all active and selected account permutations for Search (match & no-match), Refreshing, Help, and Palette (unfiltered & filtered) across all 7 views. Extracted pure state machine `tuiState.js` with comprehensive executable regression tests covering delete -> navigate, switch beta -> search, switch beta -> refresh, and switch gamma -> help/palette.
11. Persistent reduced collection fidelity & plain text assertions (1293 fixtures): Added persistent account-set and profile-set discriminators to fixture resolution (`accounts-no-alpha`, `accounts-no-beta`, `accounts-no-gamma`, `profiles-no-work`). Generated authoritative Go fixtures across all reachable active/selected combinations for Dashboard, Quota, Search, Refreshing, Help, and Palette for reduced collections, ensuring deleted accounts and removed profiles remain absent from the terminal when navigating to Quota or returning to Dashboard/Profiles. Updated test suite to assert literal absence of deleted accounts and profiles from `fixture.plain` text lines.
12. Systematic active-context completeness across all views, modes, and forms (2922 fixtures): Audited and resolved active header fidelity across all 7 views (Profiles, History, Settings, Doctor, Backup, Dashboard, Quota) and all operational modes (forms across 11 kinds, confirms, overlays, and Doctor states). Every generated fixture across all 4 account collection contexts now embeds the exact active account in `s.active` and matches the authentic `tuiActiveLine` header. Automated active line consistency assertions inside `TestTUIWebFixtures` ensure zero metadata/plain text divergence. Enriched test suite with end-to-end assertions verifying `ACTIVE` header stays Beta/Gamma across secondary view transitions, profile edits, settings forms, command history clears, backup exports, and doctor runs. Full fixture bank loading moved to browser idle time (`requestIdleCallback`) / explicit interaction to guarantee zero main-thread contention on page load.
13. Interaction fidelity completeness & exact Go behavioral contracts (3639 fixtures): Implemented complete view preservation across all operational modes (Quota confirm delete, Search on all 7 views, Update confirm on all 7 views). Mapped exact delete semantics across all 9 active × deleted account combinations with Go `clampSelection` alignment and accurate metadata. Added exact selected-profile semantics for personal vs work editing (`profile-0` vs `profile-1`) and removal with both `profiles-no-work` and `profiles-no-personal` contexts. Added full keyboard parity matching production Go (`selectNext` algorithm for `'n'`, PageUp/Down steps by view type, search editing with escape restore, and palette action routing).
14. Production delete-active contract, 21-shard dynamic loading architecture, form modal Tab forwarding, and responsive continuous clamp (5505 fixtures):
    - Aligned active deletion directly with production Go `internal/app/tui.go` (`performDelete`): deleting the active account yields `active-none`, rendering `ACTIVE  — no saved session` across all 7 views until an explicit switch occurs.
    - Partitioned the 5,505 compiled fixtures into 21 layout + view shards (`{layout}.{view}.json`, 7.5–83.7 kB gzip), completely removing the aggregate 25.8MB bank from initial page load. Initial render bundles only 3 canonical Dashboard frames (`tui-initial-fixtures.json`); shards load lazily on explicit intent with `aria-busy` and zero wrong-view fallback.
    - Added form mode Tab/Shift-Tab forwarding in `TuiTerminal` and renderer-backed bounded option cycling (`NAV_LEFT`/`NAV_RIGHT` on profile family options).
    - Removed 960→961px width inversion using a continuous `min(92%, 860px)` clamp strategy, and updated compact renderer geometry to 48×14 (innerCols 46) with `48 / 28` aspect-ratio, guaranteeing font size ≥10px at 320px and ≥11px at 375px.
15. Exact production keyboard routing, search visible query state, and palette execution fidelity (34710 fixtures):
    - Replaced hardcoded key switches with `routeKeyAction` pure router matching `internal/app/tui.go`: `?` opens Help only; `h`, `p`, `s` open History/Profiles/Settings via transaction; `o` navigates to Doctor and triggers run; `b` routes to binding form on Settings, Backup view on Dashboard, and Dashboard on secondary views; `v` routes to verify form on Backup, Quota view on other views; `r` triggers doctor on Doctor or quota refresh on Dashboard/Quota; Home/End perform boundary jumps distinct from PageUp/PageDown delta steps; external actions (`a`, `t`, `l`, `m`) announce truthful safe preview notices.
    - Implemented renderer-backed Search query state across 15 exact query slugs (`""`, `a`, `al`, `alp`, `alpha`, `b`, `be`, `bet`, `beta`, `g`, `ga`, `gam`, `gamma`, `z`, `zz`) with real-time selection clamping and visible `/<query>▌` prompt in `f.plain`.
    - Implemented renderer-backed Palette query and index states across 6 exact query slugs (`""`, `p`, `pr`, `pro`, `prof`, `profile`) and all enabled indices, with visible `:<query>▌` prompt and orange `>` cursor in `f.plain`. Enter triggers transactional navigation, form opening, action execution, or honest safe preview notices.
16. Tab propagation isolation, authoritative 48×14 compact geometry, and final Chrome parity verification:
    - Fixed Tab/Shift+Tab double-routing root cause where `TuiTerminal.handleKeyDownInternal` handled Tab by dispatching `NAV_DOWN`/`NAV_UP` but event bubbled to `InteractiveDemo`'s wrapper `onKeyDown` and routed a second time; resolved via `e.stopPropagation()` in both form mode and modal branches while preserving modal focus trapping and Shift+Tab navigation.
    - Corrected compact showcase geometry from 16:9 / 48:28 to `.deviceFrame[data-layout="compact"]` (aspect-ratio 4:3) and `.terminalOverlay` (aspect-ratio 376:228), matching the authentic 48×14 monospace character-cell dimensions.
    - Chrome-measured effective font sizes verified at 10.393px at 320px (target ≥10px) and 12.181px at 375px (target ≥11px), with no document overflow across 320–1536px and continuous width 860px at both 960px and 961px.
    - Full Chrome parity flows passed across all 34,710 fixtures and 21 dynamic shards; initial page network has zero shard requests. Tests verified: 4 worker + 28 app tests (`npm test`), targeted Go fixture tests (`TestTUIWebFixtures`), and `npm audit` 0 high vulnerabilities.

17. Release evidence: renderer-authoritative generation from `internal/app.(*Application).tuiLines`; 50,781 fixtures across 21 lazy layout-view shards (3 layouts x 7 views); all account subset and empty contexts; all profile subset and empty contexts; generalized form option states; sequential deletion/removal to true empty states; deterministic generation; Doctor and refresh async completion; exact help/modal keyboard semantics; boolean 3D activation plus graceful no-WebGL behavior; responsive toolbar anti-shrink and 320-1536 geometry; final browser CDP flows passing; npm build main 267.98 kB raw / 77.04 kB gzip, CSS 10.81 kB / 2.90 kB gzip, 3D chunk 896.92 kB / 241.91 kB gzip and lazy; npm tests 37 app + 4 worker passing; npm audit 0 vulnerabilities; Go TestTUIWebFixtures passing in 47.567s; Lighthouse local final mobile runs scores 100/100/100/100, 100/100/100/100, 99/100/100/100 with FCP 1.4s, LCP 1.4-1.5s, TBT 10-80ms, CLS 0; desktop 100/100/100/100 with FCP/LCP 0.4s, TBT 0, CLS 0; zero initial lazy-shard requests.
18. Final edge parity patch & release evidence (51,054 fixtures):
    - Incorporated final edge parity fixes: exact native History clear title "Clear local history" and History post-clear state, empty Profiles e/d/Enter guards, exact native Removed account copy, Doctor healthy completion, Go `clampSelection` after delete, sequential result banners through empty states, and option defaults/reset handling.
    - Authoritative fixture generation: exactly 51,054 renderer-authoritative fixtures partitioned into 21 lazy layout-view shards (3 layouts × 7 views) with 3 initial frames (`tui-initial-fixtures.json`).
    - Build assets: Production build main `index-DAEKmpKw.js` 268.75 kB raw / 77.24 kB gzip, CSS 10.81 kB raw / 2.90 kB gzip, lazy 3D chunk 896.92 kB raw / 241.91 kB gzip; zero initial shard requests and zero initial 3D requests.
    - Test & verification: Automated test suite passing with 38 app + 4 worker tests (`npm test`), `npm audit` 0 vulnerabilities, normal Go `TestTUIWebFixtures` passing; Chrome CDP parity, QA, release, expanded, and adversarial suites all passing; final recaptured screenshots 13 and same-view composite.
    - Final post-fix Lighthouse verification: Verified against `lighthouse-final-current-mobile-1.json`, `lighthouse-final-current-mobile-2.json`, `lighthouse-final-current-mobile-3.json`, and `lighthouse-final-current-desktop.json`. All four categories 100 each (100/100/100/100 across mobile and desktop); mobile FCP/LCP 1.4s, TBT 40/0/0ms, CLS 0; desktop FCP/LCP 0.3s, TBT 0, CLS 0.

## Release checks

- Mobile Lighthouse (3 consecutive runs): 100/100/100/100, 100/100/100/100, 100/100/100/100 with FCP/LCP 1.4s, TBT 40/0/0ms, CLS 0 (`lighthouse-final-current-mobile-1/2/3.json`, all four categories 100 each).
- Desktop Lighthouse: 100/100/100/100 with FCP/LCP 0.3s, TBT 0, CLS 0 (`lighthouse-final-current-desktop.json`, all four categories 100 each).
- Automated Tests: 38 app + 4 worker passing (`npm test`), normal Go `TestTUIWebFixtures` passing, `npm audit` 0 vulnerabilities, and Chrome CDP parity/QA/release/expanded/adversarial all passing.
- Visual Verification: Final recaptured screenshots 13 and same-view composite verified across wide, stacked, and compact views.

## TUI Fidelity & Authoritative Renderer Integration

- Authoritative Source: All browser terminal screens are directly sourced from the Go application renderer (`internal/app.(*Application).tuiLines`) across 51,054 compiled fixtures partitioned into exactly 21 lazy layout-view shards and 3 initial frames (`tui-initial-fixtures.json`), completely eliminating bespoke HTML mockups, fake status bars, and invented confirmation dialogs.
- Performance Architecture: Instant zero-CLS initial render from 3-frame initial bank (`tui-initial-fixtures.json`); dynamic on-demand loading of individual layout+view shards. Production build main `index-DAEKmpKw.js` is 268.75 kB raw / 77.24 kB gzip. CSS is 10.81 kB raw / 2.90 kB gzip, lazy 3D chunk is 896.92 kB raw / 241.91 kB gzip. Zero aggregate asset fetch on initial page load; zero initial shard requests and zero initial 3D requests.
- Fixed Character-Cell Architecture: Monospace cell capacity (`cols`, `rows`) measured directly from aperture and monospace metrics to trigger wide (>=92×18), stacked, and compact (<64×16) layout selection with matched CSS aspects (wide 100/48, stacked 80/48, compact 376/228 with compact frame 4:3) centered in the carbon aperture.
- Real Runtime Interaction Model: Real account-switch workflows immediately switch active accounts in all 7 views and modal contexts across all reachable active×selected pairs. Genuine post-delete fixtures reflect actual account removal with persistent reduced collection fidelity and authentic `active-none` states. Final edge parity fixes guarantee exact native History clear title "Clear local history" and History post-clear state, empty Profiles e/d/Enter guards, exact native Removed account copy, Doctor healthy completion, Go `clampSelection` after delete, sequential result banners through empty states, and option defaults/reset handling.
- ANSI SGR Compliance: Safe parser parses whitelisted SGR sequences (0 reset, 1 bold, 22 normal, 38;5;n 256 foreground colors, 39 default) directly into React text and styled spans without `dangerouslySetInnerHTML`.
- Accessibility & Precision: Screen reader plain text representations, `aria-live` interactive announcements, focus-visible terminal region, dynamic `role="dialog"` with Tab-trapping during modal overlays, and accessible toolbar controls.

passed
