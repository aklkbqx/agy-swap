# agy-swap showcase

React, TypeScript, Tailwind CSS and Vite source for the GitHub Pages site.

```bash
npm ci
npm run lint
npm run build
```

The build writes to `../docs`. Commit the generated `docs` changes with the source changes; CI rebuilds the site and rejects drift.
